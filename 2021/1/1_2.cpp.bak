#include <bits/stdc++.h>

using namespace std;

int main() {
	
	ifstream fin("1.in");
	ofstream fout("1_2.out");
	
	int first = -1, sum = 0;
	int x, ans = 0;
	for (int i = 0; i < 3; i++) {
		fin >> x;
		if (!i) {
			first = x;
		}
		sum += x;
	}
	while (fin >> x) {
		if (x > first) {
		}
	}
	fout << ans << endl;
	
	return 0;
}